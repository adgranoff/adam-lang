// Recursive Fibonacci — the classic benchmark.
// Demonstrates: functions, recursion, conditionals, arithmetic.

fn fibonacci(n) {
    if n <= 1 {
        n
    } else {
        fibonacci(n - 1) + fibonacci(n - 2)
    }
}

println(fibonacci(20))
// expect: 6765
