// Benchmark: string concatenation
// Stress-tests GC pressure from string allocation.

fn build_string(n) {
    let s = ""
    let i = 0
    while i < n {
        s = s + "x"
        i = i + 1
    }
    s
}

let result = build_string(10000)
println(len(result))
