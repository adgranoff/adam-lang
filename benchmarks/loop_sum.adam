// Benchmark: tight loop summation
// Tests basic VM dispatch speed (OP_ADD, OP_LT, OP_LOOP).

fn sum_to(n) {
    let total = 0
    let i = 0
    while i < n {
        total = total + i
        i = i + 1
    }
    total
}

println(sum_to(1000000))
