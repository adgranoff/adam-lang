{
  "name": "adam-playground",
  "version": "0.1.0",
  "description": "Browser-based playground for the Adam programming language with Monaco editor.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "monaco-editor": "^0.44.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
